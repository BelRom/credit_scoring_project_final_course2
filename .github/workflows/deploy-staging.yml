name: Deploy Staging

on:
  push:
    branches: [develop]
  workflow_dispatch:

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - name: Skip staging deployment
        run: echo "Staging deployment is skipped."
